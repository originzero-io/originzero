// import colors from "colors";
// import socketIoLogger from "socket.io-logger";

// const options = {
//   stream: {
//     write: function (data: any) {
//       console.log(`SOCKET EVENT: ${data}`.cyan);
//     },
//   },
//   format: function (sock: { id: number; }, args: any[]) {
//     return { socket: sock.id, event_name: args[0] };
//   },
// };
// export default socketIoLogger(options);
